import React, { Component } from 'react';
import firebase from './firebase.js';
import './RacePoints.css'

class RacePoints extends Component {
    render() {
      return (
        <section className="RacePoints">
            <h1>Pace Points Section</h1>
        </section>
      );
    }
  }

export default RacePoints;
import firebase from './firebase.js';

export default RacePoints;
